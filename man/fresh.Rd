% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nutrition_fresh.R
\name{fresh}
\alias{fresh}
\title{Forage Resource Evaluation System for Habitat (FRESH)}
\usage{
fresh(
  biomass_data,
  nutrition_data,
  biomass_unit = c("g", "kg"),
  de_unit = c("kj_g", "kcal_g"),
  dmi_unit = c("g_day", "kg_day"),
  output_area_unit = c("kg_ha", "g_m2", "g_900m2", "kg_m2", "g_ha"),
  area = NULL,
  animal_de_req = 11.5,
  animal_dp_req = 7.5,
  animal_dmi = NULL,
  max_any_forage_prop = 1,
  min_total_biomass = 0,
  warnings = T
)
}
\arguments{
\item{biomass_data}{Long-format data frame with one row per forage per
patch.
Required columns: \code{plant_id} (character), \code{biomass} (numeric).
Optional columns:
\itemize{
\item \code{patch_id} (character or factor) -- groups rows into patches; one LP
is solved per patch. If absent, all rows are treated as a single
patch and a warning is issued.
\item \code{plant_part} or \code{plant_phenology} (character) -- subdivides a species
into parts with different nutritional values (e.g., leaves vs.
twigs). Must match the column name used in \code{nutrition_data}. If
absent in both tables, the join uses \code{plant_id} only.
\item \code{area} (numeric, m^2) -- area over which biomass was collected. Must
be constant within a patch. Overridden by the \code{area} argument if
both are supplied.
}}

\item{nutrition_data}{Lookup data frame, one row per plant_id (and part).
Required columns: \code{plant_id} (character), \code{de} (numeric), \code{dp}
(numeric), \code{diet_proportion} (numeric, 0--1).
Optional columns:
\itemize{
\item \code{plant_part} or \code{plant_phenology} (character) -- must match the
column name used in \code{biomass_data}.
\item \code{sd_de} and \code{sd_dp} (numeric) -- standard deviations of \code{de} and
\code{dp}. Both must be present or neither. If present, each forage is
expanded into low/mid/high SD groups in the LP (proportions 0.16 /
0.68 / 0.16 from the normal distribution), allowing partial
inclusion of forages near a nutritional threshold. Forages with \code{NA}
SD values stay as a single (mean) group.
}}

\item{biomass_unit}{Unit of \code{biomass} column. \code{"g"} (default) or \code{"kg"}.}

\item{de_unit}{Unit of \code{de} column and \code{animal_de_req}. \code{"kj_g"} (default)
or \code{"kcal_g"}.}

\item{dmi_unit}{Unit of \code{animal_dmi}. \code{"g_day"} (default) or \code{"kg_day"}.}

\item{output_area_unit}{Unit for output biomass densities and animal days.
One of \code{"kg_ha"} (default), \code{"g_m2"}, \code{"g_900m2"}, \code{"kg_m2"}, or \code{"g_ha"}.}

\item{area}{Scalar area in m^2 over which biomass was collected. If
supplied and \code{biomass_data} has an \code{area} column, the argument wins (with
warning). At least one of the two must be present.}

\item{animal_de_req}{Minimum digestible energy concentration of the diet,
in units matching \code{de_unit}. Scalar or numeric vector. Default 11.5
(kJ/g, corresponding to a moderate-quality diet of roughly 60\% dry
matter digestibility for a generalist herbivore). When \code{animal_de_req}
and \code{animal_dp_req} are both vectors they must be the same length, and
each pair (\code{animal_de_req[i]}, \code{animal_dp_req[i]}) is a separate LP
solve per patch, producing a separate row in \code{summary} and a separate
set of rows in \code{detail}. If one is scalar and the other is a vector,
the scalar is recycled to match the vector's length.}

\item{animal_dp_req}{Minimum digestible protein concentration of the
diet, in g/100g of dry matter. Scalar or numeric vector; recycling and
vector behavior as described for \code{animal_de_req}. Default 7.5 (an
intermediate value between body-maintenance and lactation requirements
for a medium-sized cervid).}

\item{animal_dmi}{Daily dry matter intake, in units matching \code{dmi_unit}.
Optional; if \code{NULL}, \code{animal_days_per_area} in the output is \code{NA}.}

\item{max_any_forage_prop}{Scalar in [0, 1]. Caps the proportion of any
single forage (summed across SD groups) in the diet. Intended to enforce
dietary diversity for generalist herbivores that cannot tolerate any
single forage dominating their diet. Default 1 (no effect).}

\item{min_total_biomass}{Scalar >= 0, in \code{output_area_unit}. Subtracted
from total suitable biomass before computing animal days. Acts as a
foraging efficiency floor: below some biomass density, herbivores cannot
forage profitably regardless of forage quality. Default 0 (no effect).}

\item{warnings}{Logical. If \code{TRUE} (default), warnings issued during the
call are printed to the console as usual. If \code{FALSE}, they are
suppressed from the console. Either way, all warning messages are
collected and returned in the \code{warnings} element of the output object.}
}
\value{
An object of class \code{"fresh"}: a list with the following components.
\itemize{
\item \code{summary} -- a tibble with one row per patch x constraint pair.
Columns: \code{patch_id}, \code{de_req}, \code{dp_req}, \code{suitable_biomass},
\code{animal_days_per_area}, \code{mean_de}, \code{mean_dp}, \code{n_forages_used},
\code{total_biomass_available}, \code{limiting_constraint}. A logical
\code{infeasible} column is added only if at least one row was
infeasible.
\item \code{detail} -- a tibble with one row per forage per patch per
constraint pair (SD groups collapsed back to the biological forage).
Columns: \code{patch_id}, \code{de_req}, \code{dp_req}, \code{plant_id}, \code{plant_part} or
\code{plant_phenology} (if present in input), \code{biomass_available},
\code{biomass_used}, \code{prop_used}, \code{prop_of_total}.
\item \code{call} -- the matched call.
\item \code{inputs} -- a named list of the resolved argument values (units,
area, constraints, and an internal flag indicating whether
nutritional variation was active).
\item \code{data} -- a list with \code{biomass_data} and \code{nutrition_data} as supplied
by the user.
\item \code{warnings} -- a character vector of warning messages issued during
the call, or the string \code{"no warnings"} if none fired.
}
}
\description{
Computes the maximum suitable forage biomass that meets minimum digestible
energy and digestible protein concentration constraints for a generalist
herbivore, using linear programming.
}
\details{
This implementation follows the linear-programming formulation of Hanley
et al. (2012) but departs from the original in the following ways:
\itemize{
\item No summer-to-winter conversion. The function is season-agnostic; users
pre-convert biomass and nutritional values for winter analysis.
\item No snow submodel. The original derives snow depth from elevation,
slope, aspect, and canopy cover; this is not implemented.
\item No landscape/GIS application. Only stand-level analysis is supported.
Each \code{patch_id} is solved independently.
\item Nutritional variation uses normal-distribution probability mass for
the low/mid/high SD groups (0.16 / 0.68 / 0.16) rather than the equal
thirds (1/3 / 1/3 / 1/3) used by Hanley et al. The probability-based
discretization is statistically consistent with the assumption of
normality implied by reporting SDs in the first place.
\item Infeasible LP solves return \code{NA} rather than 0, with an \code{infeasible}
column added to the summary.
\item \code{min_total_biomass} is specified in the user's chosen
\code{output_area_unit} rather than fixed at kg/ha.
}
}
\references{
Hanley, T.A.; Spalinger, D.E.; Mock, K.J.; Weaver, O.L.; Harris, G.M. 2012.
Forage resource evaluation system for habitat--deer: an interactive deer
habitat model. Gen. Tech. Rep. PNW-GTR-858. Portland, OR: U.S. Department
of Agriculture, Forest Service, Pacific Northwest Research Station. 64 p.
\url{https://research.fs.usda.gov/treesearch/40300}
}
\seealso{
\code{\link[lpSolve]{lp}} for the underlying linear-programming
solver used internally.
}
