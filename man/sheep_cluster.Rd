% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eco_sheep_cluster.R
\name{sheep_cluster}
\alias{sheep_cluster}
\title{Cluster recent sheep GPS locations into search areas}
\usage{
sheep_cluster(
  GPS,
  no.days = 7,
  reference_date = Sys.time(),
  minimum.points = 10,
  epsilon = 100,
  crs = "EPSG:32612",
  filetype = "KML",
  last.point = TRUE,
  trace = TRUE,
  basic.plot = TRUE
)
}
\arguments{
\item{GPS}{A data frame containing columns \code{x}, \code{y}, \code{ID}, and \code{DateTime}.
\code{x} and \code{y} should be projected coordinates in the supplied \code{crs}.
\code{DateTime} must be a \code{POSIXct} vector.}

\item{no.days}{Numeric. Number of days before \code{reference_date} to retain.}

\item{reference_date}{A \code{POSIXct} reference date. Defaults to current time.}

\item{minimum.points}{Integer. Minimum number of points required by DBSCAN
to define a cluster.}

\item{epsilon}{Numeric. DBSCAN epsilon parameter, in map units of \code{crs}.}

\item{crs}{Character string giving the coordinate reference system, passed to
\code{\link[terra:vect]{terra::vect()}}. Defaults to \code{"EPSG:32612"}.}

\item{filetype}{Character. Output vector file type. Must be either \code{"KML"}
or \code{"GPX"}.}

\item{last.point}{Logical. If \code{TRUE}, write last locations for each
individual.}

\item{trace}{Logical. If \code{TRUE}, write movement paths for each individual.}

\item{basic.plot}{Logical. If \code{TRUE}, draw a simple plot of search areas,
movement paths, and last locations.}
}
\value{
A \code{SpatVector} of search areas if clusters are found. If no clusters
are found, returns the point \code{SpatVector} with attached \code{clusterID}.
}
\description{
Creates recent last locations, movement paths, and DBSCAN-based convex hull
search areas from GPS data. Optionally writes outputs as KML or GPX and
produces a basic plot.
}
\details{
The function filters GPS locations to observations occurring within
\code{no.days} before \code{reference_date}, then applies DBSCAN clustering to all
retained points. Minimum convex hulls are drawn around non-noise clusters.

Output files are written to the working directory as:
\itemize{
\item \verb{last_locations.<ext>}
\item \verb{movement_paths.<ext>}
\item \verb{search_areas.<ext>}
}

where \verb{<ext>} is determined by \code{filetype}.

GPX support for polygon outputs depends on the software used to read the
file, and is generally less robust than KML for search-area polygons.
}
\examples{
\dontrun{
out <- sheep_cluster(
  GPS = gps_df,
  no.days = 7,
  minimum.points = 10,
  epsilon = 100,
  crs = "EPSG:32612",
  filetype = "KML"
)
}

}
