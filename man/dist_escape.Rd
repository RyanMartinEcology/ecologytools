% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dist_escape.R
\name{dist_escape}
\alias{dist_escape}
\title{Distance to escape terrain}
\usage{
dist_escape(dem, escape_slope)
}
\arguments{
\item{dem}{A \code{terra::SpatRaster} representing elevation.}

\item{escape_slope}{Numeric scalar giving the slope threshold in degrees.}
}
\value{
A \code{terra::SpatRaster} giving the distance in meters from each cell
to the nearest escape-terrain cell.
}
\description{
Calculates the distance from each raster cell to the nearest cell meeting
or exceeding a specified slope threshold, interpreted as escape terrain.
}
\details{
Slope is calculated from the input DEM using \code{terra::terrain()} with
8 neighbors and units in degrees. Cells with slope greater than or equal
to \code{escape_slope} are treated as escape terrain.
}
\examples{
\dontrun{
library(terra)

r <- rast(nrows = 100, ncols = 100, xmin = 0, xmax = 100, ymin = 0, ymax = 100)
values(r) <- runif(ncell(r), 0, 1000)

d <- dist_escape(r, escape_slope = 30)
names(d)
plot(d)
}

}
